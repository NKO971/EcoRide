// Attendre que le DOM soit complètement chargé
document.addEventListener('DOMContentLoaded', () => {
    console.log("✅ Page admin chargée - Authentification gérée par le serveur PHP");
    
    // Aucune logique mock n'est nécessaire
    // Le serveur PHP gère:
    // - L'authentification et vérification du rôle
    // - Les données utilisateurs de la session
    // - L'affichage des statistiques et graphiques
    // - La création d'employés
});