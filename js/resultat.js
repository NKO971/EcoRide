document.addEventListener('DOMContentLoaded', () => {
    console.log("✅ Page résultats chargée");
    
    // NOTE: Les trajets sont chargés depuis le backend PHP
    // Les filtres et la recherche sont traités côté serveur
});